{% block title %}
{%  endblock %}
{%  block content %}
<h1>
    {#{{  'title.tags_list'|trans }} #}
    Tagi
</h1>

{# some comment #}
{% block body %}
    <h1>
        {{ 'title.tag_list'|trans }}
    </h1>
    {% if tags |length %}
        {% for row in tags %}
            <li> {{ row.tag_id }}.{{ row.name }}</li>
        {%  endfor %}
    {% else %}
        <div>
            No tag found!
        </div>
    {% endif %}
{%  endblock %}

{%  endblock %}